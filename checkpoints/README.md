# Checkpoints

Place trained model weights here, for example:

- `multitask_model.pth`
- `best_encoder.pt`

Large model files are ignored by Git through `.gitignore`. For GitHub releases, upload checkpoints as release assets or publish them through a model hub.
